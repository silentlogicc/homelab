# Hello-World Docker Test   ← H1 heading

this container checks if Docker is installed and working

---

## Useful markdown examples   ← H2 heading

**1) bold:**  
`**text**` → **text**

*2) italic:*  
`*text*` → *text*

**3) code block (bash):**
```bash
sudo docker run hello-world
